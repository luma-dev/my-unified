import { someDef } from "./someDef.txt";

{
(() => {
let x = 4;
return x;
})()
}

<KatexDef _={someDef} />
<KatexDef _="foo" />
<KatexDef _={"bar"} />

```katex-def
\def\idmat{I}
\gdef\Tr#1{\operatorname{Tr}\left[{#1}\right]}
```

```katex
\Tr \idmat = 1
```

```katex trans
1 + 1
= S(O) + S(O)
= S(O + S(O))
= S(S(O))
```

<KatexReset />

```katex {1,2,3} {1-2} lang=js
\Tr \idmat = 1
```
